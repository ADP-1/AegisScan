# Core dependencies
requests==2.31.0
beautifulsoup4==4.12.2  # for web scraping
colorama==0.4.6  # for colored terminal output
argparse==1.4.0  # command line argument processing 
setuptools>=75.0.0  # for package installation

# Additional dependencies for CSRF scanner
urllib3==2.0.7  # HTTP client (used by requests)
lxml==4.9.3  # HTML parser (used by BeautifulSoup)
soupsieve==2.5  # CSS selector for BeautifulSoup

# Testing
pytest==8.1.1 

# Add other dependencies for sqlmap/xsstrike integration 
# These are typically installed separately as they are external tools